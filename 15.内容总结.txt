1 mvc 与 mvvm的区别
2 插值表达式 v-cloak v-text v-html v-bind(缩写为':') v-on(缩写为'@')
3 v-model v-for v-if v-show
4 时间修饰符 : .stop .prevent .capture .self .once
5 el 指定要控制的区域 data:是个对象，指定控制区域内要用到的数据 methods 虽然
带一个s后缀，但是是一个对象，这里定义了一些自定义的方法
6 在vm实例中 如果需要访问data上的数据，或者要访问methods中的方法，必须带this
7 在v-for中 要学会使用key属性，只接受key属性(只接受string/number)
8 v-model 只能应用与表单元素
9 在vue中绑定样式的两种方法,v-bind:calss v-bind:style

